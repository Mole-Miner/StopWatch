export enum NumberKey {
    SECONDS = "seconds",
    MINUTES = "minutes",
    HOURS = "hours"
}

export enum NumberSchedule {
   SECOND = 1,
   MINUTE = 60,
   HOUR = 3600
}

export enum NumberEdge {
    SIXTY_ONE = 61,
    TWENTY_FIVE = 25
}

export enum NumberDue {
    DUE = 0
}

export enum NumberSpeed {
    MILLISECONDS = 1000
}

